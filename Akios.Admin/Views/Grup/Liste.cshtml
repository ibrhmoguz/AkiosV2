@{
    Layout = null;
}

<script type="text/javascript">
    $(function () {
        $("#masterTitle").html("Grup");
        $("#masterDescription").text("Sistemde tanımlı kullanıcı gruplarını listeler.");
        LoadListData();
    });

    function LoadListData() {
        $("#tableGruplar").dataTable({
            "sAjaxSource": "Grup/JsonList",
            "aaSorting": [[1,'asc']],
            "aoColumnDefs": [
            {
                "aTargets": [0],
                "bVisible":false
            },
            {
                "aTargets": [3],
                "bSortable": false,
                "mRender": function (data, type, row) {
                    return '<input type="button" class="btn btn-block btn-info" value="Düzenle" GrupId ="' + row[0] + '"/>';
                }
            },
            {
                "aTargets": [4],
                "bSortable": false,
                "mRender": function (data, type, row) {
                    return '<input type="button" class="btn btn-block btn-danger" value="Sil" GrupId ="' + row[0] + '"/>';
                }
            }]
        });
    }
</script>
<div class="row">
    <div class="col-xs-12">
        <div class="box box-primary">
            <div class="box-body">
                <table id="tableGruplar" class="table table-bordered table-hover">
                    <thead>
                        <tr>
                            <th></th>
                            <th>Adı</th>
                            <th>Müşteri</th>
                            <th style="width:8%"></th>
                            <th style="width:8%"></th>
                        </tr>
                    </thead>
                    <tbody></tbody>
                </table>
            </div>
        </div>
    </div>
</div>

