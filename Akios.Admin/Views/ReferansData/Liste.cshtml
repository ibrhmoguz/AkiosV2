@{
    Layout = null;
}

<script type="text/javascript">
    $(function () {
        $("#masterTitle").html("Referans");
        $("#masterDescription").text("Sistemde tanımlı referansları listeler.");
        LoadListData();
    });

    function LoadListData() {
        $("#tableReferansDatalar").dataTable({
            "sAjaxSource": "ReferansData/JsonList",
            "aaSorting": [[1,'asc']],
            "aoColumnDefs": [
            {
                "aTargets": [0],
                "bVisible":false
            },
            {
                "aTargets": [3],
                "bSortable": false,
                "mRender": function (data, type, row) {
                    return '<input type="button" class="btn btn-block btn-success" value="Detay Ekle" RefId ="' + row[0] + '"/>';
                }
            },
            {
                "aTargets": [4],
                "bSortable": false,
                "mRender": function (data, type, row) {
                    return '<input type="button" class="btn btn-block btn-info" value="Düzenle" RefId ="' + row[0] + '"/>';
                }
            },
            {
                "aTargets": [5],
                "bSortable": false,
                "mRender": function (data, type, row) {
                    return '<input type="button" class="btn btn-block btn-danger" value="Sil" RefId ="' + row[0] + '"/>';
                }
            }]
        });
    }
</script>
<div class="row">
    <div class="col-xs-12">
        <div class="box box-primary">
            <div class="box-body">
                <table id="tableReferansDatalar" class="table table-bordered table-hover">
                    <thead>
                        <tr>
                            <th></th>
                            <th>Müşteri</th>
                            <th>Referans</th>
                            <th style="width:8%"></th>
                            <th style="width:8%"></th>
                            <th style="width:8%"></th>
                        </tr>
                    </thead>
                    <tbody></tbody>
                </table>
            </div>
        </div>
    </div>
</div>

